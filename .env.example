# Local development defaults
DATABASE_URL=postgresql://localhost:5433/pv_schedule
PORT=5000
HOST=0.0.0.0
REUSE_PORT=false

# Storage selection flags
USE_IN_MEMORY_STORAGE=false
USE_SAMPLE_DATA=false
SEED_DEMO_DATA=true

# Uploads (local disk by default)
UPLOADS_ROOT=
MEDIA_BASE_URL=
UPLOADS_ENABLED=true
VITE_UPLOADS_ENABLED=true

# Production data mirroring (optional)
USE_PRODUCTION_DATA=false
PRODUCTION_DATABASE_URL=
PRODUCTION_API_BASE_URL=
